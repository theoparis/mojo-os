# Mojo OS

An experimental OS written with Mojo, because why not.

## Building and Running with QEMU

**First, you will need a patched build of Mojo that supports debug assertions on bare metal hardware - see [mojo.patch](./mojo.patch).** You will also need QEMU and GNU Make.

Running it should be as simple as:

```sh
make run
```
